groups:
  - name: service-health
    interval: 15s
    rules:
      - alert: ServiceDown
        expr: up == 0
        for: 15s
        labels:
          severity: heal
        annotations:
          summary: "Service {{ $labels.job }} is down"
          description: "The service {{ $labels.job }} has been unreachable for more than 15s."
